### Command: cd ~ |  mkdir Question2  | cd Question2
Created and navigated into the Question2 directory to store the log analysis script.

### Command: nano sample.log
Created a sample log file to test the script functionality.

### Command: nano log_analyze.sh
Wrote a shell script that validates a log file, counts log levels, extracts the latest error, and generates a dated summary report.

### Command: chmod +x log_analyze.sh
Made the script executable using chmod.

### Command: ./log_analyze.sh sample.log
Executed the script on a log file. The script correctly analyzed log entries and generated a summary report.

### Command: cat logsummary_*.txt
Displayed the contents of the generated log summary report.
