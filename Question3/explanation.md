### Command: cd ~ | mkdir Question3 | cd Question3
Created and navigated into the Question3 directory to store the log analysis script.

### Command: nano marks.txt
Created a marks.txt file containing student roll numbers, names, and marks for sample testing.

### Command: nano validate_results.sh
Wrote a shell script that reads student marks, evaluates pass/fail conditions using arithmetic operations, and categorizes students accordingly.

### Command: chmod +x validate_results.sh
Changed file permissions to make the validate_results.sh script executable.

### Command: ./validate_results.sh
Executed the script, which correctly identified students who failed exactly one subject, students who passed all subjects, and displayed their respective counts.

